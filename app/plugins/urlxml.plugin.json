{
  "TypeKey": "URLXML",
  "DisplayName": "URL XML Monitor",
  "Description": "Fetches a URL and evaluates an XPath expression against the XML response",
  "Parameters": [
    { "Name": "Url", "Type": "string", "Default": "", "Required": true, "Description": "URL returning XML" },
    { "Name": "XPath", "Type": "string", "Default": "", "Required": false, "Description": "XPath expression to evaluate (result used as counter value)" },
    { "Name": "WarnValue", "Type": "decimal", "Default": 0, "Required": false, "Description": "Warning if XPath value exceeds this" },
    { "Name": "FailValue", "Type": "decimal", "Default": 0, "Required": false, "Description": "Failure if XPath value exceeds this" },
    { "Name": "TimeoutMs", "Type": "int", "Default": 10000, "Required": false, "Description": "Request timeout (ms)" }
  ]
}
